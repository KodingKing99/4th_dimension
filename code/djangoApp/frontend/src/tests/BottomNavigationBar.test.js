import {render} from '@testing-library/react';
// import BottomNavigationBar from '../componenets/BottomNavbar/BottomNavigationBar';
describe("", () => {
    // beforeEach(() => {
        
    // })
    test("Should render different buttons based on users role", () => {
        expect(true).toBe(true);
        expect(true).toBe(true);
        expect(true).toBe(true);
        expect(true).toBe(true);
        expect(true).toBe(true);
    }),
    test('Should clicking on the icon should navigate you to another page', () => {
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
    })

})