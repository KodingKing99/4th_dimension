describe("", () => {
    test("Should allow a player to enter in a tournament", () => {
        expect(true).toBe(true);
    }),
    test('Page should render a Hole #, Par, next and back button ', () => {
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
    })

})