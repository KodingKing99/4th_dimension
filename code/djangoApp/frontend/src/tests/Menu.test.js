describe("", () => {
    test("Should list menu items", () => {
        expect(true).toBe(true);
    }),
    test('Clicking on menu item should bring up cart', () => {
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
    }),
    test('Clicking on menu item should bring up cart', () => {
        expect(false).toBe(false);
        expect(false).toBe(false);
        expect(false).toBe(false);
    })
})